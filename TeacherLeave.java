package college.management.system;

import javax.swing.*;

public class TeacherLeave extends JFrame {
    public TeacherLeave() {
        setTitle("TeacherLeave");
        setSize(400, 300);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
