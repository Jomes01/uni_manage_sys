package college.management.system;

import javax.swing.*;

public class StudentLeave extends JFrame {
    public StudentLeave() {
        setTitle("StudentLeave");
        setSize(400, 300);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
