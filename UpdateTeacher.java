package college.management.system;

import javax.swing.*;

public class UpdateTeacher extends JFrame {
    public UpdateTeacher() {
        setTitle("UpdateTeacher");
        setSize(400, 300);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}
